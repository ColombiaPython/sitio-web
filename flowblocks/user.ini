[block]
name = Bloque Usuario
button_label = Usuario

[fields.username]
label = Usuario
type = select
source = site.query('/usuarios').filter(F.type == 'persona')

[fields.description]
label = Descripcion
type = string
